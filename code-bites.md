# Quick Start

## Database

Run
`docker run --name budmon-postgres -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=anypasswordhere -e POSTGRES_DB=budmon -p 9001:5432 -d postgres`
in the terminal to setup a the database
